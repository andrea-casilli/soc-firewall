# SOC Firewall Production Requirements
# Optimized for production deployment
# Version: 1.0.0

# Include base requirements
-r base.txt

# Production-specific optimizations
gunicorn==21.2.0  # Production WSGI server
gevent==24.2.0    # Async workers for gunicorn
uvloop==0.19.0    # Faster event loop
httptools==0.6.1  # Fast HTTP parsing

# Database connection pooling
pgbouncer==1.21.0  # PostgreSQL connection pooler (if using PostgreSQL)

# Redis for caching and rate limiting
redis==5.0.1
hiredis==2.2.3  # Faster Redis parser
redis-py-cluster==2.1.3

# Message queue
celery==5.3.4
kombu==5.3.4
billiard==4.1.0

# Monitoring and profiling
elastic-apm==6.17.0  # Elastic APM agent
datadog==0.46.0      # Datadog integration
newrelic==9.6.0      # New Relic integration
statsd==4.0.1        # StatsD client

# Metrics export
prometheus-client==0.19.0
graphite-exporter==0.1.0

# Distributed tracing
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-exporter-otlp==1.21.0
opentelemetry-instrumentation-flask==0.42b0

# Production database drivers
psycopg2-binary==2.9.9      # PostgreSQL (optional)
pymysql==1.1.0              # MySQL (optional)
asyncpg==0.29.0             # Async PostgreSQL (optional)

# Connection pooling
sqlalchemy-pool==1.0.0

# Production caching
cachelib==0.11.0
python-memcached==1.59

# Load balancing and failover
haproxy==1.0.0  # Python wrapper for HAProxy stats

# SSL/TLS
certifi==2023.11.17
pyOpenSSL==23.3.0

# Process management
supervisor==4.2.5  # Process control

# Health checks
healthcheck==2.0.0

# Rate limiting (production)
limits==3.6.0
redis-rate-limiter==0.2.0

# Security hardening
django-axes==6.4.0  # Brute force protection (if using Django)
fail2ban==1.0.2     # Integration with fail2ban

# Secret management
python-keyring==24.3.0
keyrings.cryptfile==1.3.9
boto3==1.34.14      # AWS Secrets Manager
hvac==2.0.0         # HashiCorp Vault

# Log aggregation
python-logstash==0.4.8
fluent-logger==0.10.0

# CDN and static assets
whitenoise==6.6.0  # Static file serving

# Compression
brotli==1.1.0      # Better compression than gzip

# Distributed locks
redis-lock==0.2.0
redlock==1.2.0

# Circuit breakers
pybreaker==1.0.0

# Production utilities
python-json-logger==2.0.7
ujson==5.8.0       # Faster JSON
simplejson==3.19.2 # Alternative JSON

# Cron jobs
python-crontab==3.0.0

# Email production
premailer==3.10.0  # CSS inlining for emails

# Asset management
webassets==2.0

# Feature flags
flask-featureflags==1.0.0

# Canary deployments
flask-canary==0.1.0
